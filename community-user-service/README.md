# Community User Service

此模块为社区用户服务的 Spring Boot 基础项目。

## 包结构
- `src/main/java/com/community/user/UserServiceApplication.java` 主启动类。

## 配置
- 使用 `application.yml` 进行基础服务配置。

## 启动方法
```
mvn spring-boot:run
```
